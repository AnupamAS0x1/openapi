name: Spec PR

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  create-and-upload-yaml-artifact:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create and Download YAML File
        run: |
          mkdir -p tests
          wget -O tests/openapi-bundled.yaml https://raw.githubusercontent.com/OAI/OpenAPI-Specification/main/examples/v3.0/petstore.yaml

      - name: Upload YAML Artifact
        uses: actions/upload-artifact@v2
        with:
          name: openapi-bundled
          path: tests/openapi-bundled.yaml
